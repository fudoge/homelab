apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cert-manager
  namespace: cert-manager
spec:
  interval: 10m

  targetNamespace: cert-manager

  chartRef:
    kind: OCIRepository
    name: cert-manager
    namespace: cert-manager

  values:
    crds:
      enabled: true
