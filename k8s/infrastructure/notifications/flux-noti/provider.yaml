apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Provider
metadata:
  name: ntfy-gateway
  namespace: flux-system
spec:
  type: generic
  address: http://gateway.ntfy.svc.cluster.local/api/webhooks/flux
  secretRef:
    name: ntfy-gateway
