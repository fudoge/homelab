apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - deployment.yaml
  - ingress.yaml
  - persistent-volume.yaml
  - persistent-volume-claim.yaml
  - service.yaml
configMapGenerator:
  - name: server-config
    files:
      - server.yaml
namespace: ntfy
